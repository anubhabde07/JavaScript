const pi = 3.14159; // A constant variable, cannot be reassigned
let radius = 5; // A variable that can be reassigned

// Calculate the area of a circle
let area = pi * radius * radius;
console.log("Area of the circle:", area);


let parameter = 2 * pi * radius;
console.log("Parameter of the circle:", parameter);

let length;

console.log([area, parameter, length]);

var name = "Anubhab";
var name = "De";
console.log(name); // 'var' allows redeclaration and reassignment